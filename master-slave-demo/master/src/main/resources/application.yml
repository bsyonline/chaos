spring:
  application:
    name: netty-server
  redis:
    host: localhost
    port: 6379
    jedis:
      pool:
        max-idle: 100
        max-wait: 1000s
        max-active: -1
        min-idle: 1
server:
  port: 8080
tcp:
  port: 8888
node:
  id: 1
  type: server